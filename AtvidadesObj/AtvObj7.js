/*Dado o array let numeros = [1, 2, 3, 4, 5], use o método shift() 
para remover o primeiro elemento do array. Exiba o array resultante e o elemento removido. */

const numeros = [1,2,3,4,5]
console.log(numeros)

numeros.shift(0)
console.log(numeros)